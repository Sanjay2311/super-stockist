# Ponytail Debt Ledger

Deliberate corners cut, each with its ceiling and the task that clears it.

| Date | Item | Ceiling | Upgrade path | Cleared by |
|------|------|---------|--------------|------------|
| 2026-08-31 | No CI pipeline | Tests only run locally | Add GitHub Actions running `npm test` + `npm run e2e` | Post-M1 |
| 2026-08-31 | Hand-written empty `drizzle/meta/_journal.json` | `migrate()` needs the journal to exist; there are no migrations yet so it is a stub with `entries: []` | Task 3 runs `drizzle-kit generate` for the first schema, which owns and rewrites this file | Cleared by Task 3 — `db:generate` regenerated `drizzle/meta/*` + `0000_eminent_stingray.sql` |
| 2026-08-31 | Single Drizzle client in `src/server/db/client.ts` switched by the `VITEST` env var (test run → `TEST_DATABASE_URL`, else `DATABASE_URL`) | One DB per process — the app client and the test client cannot both be live in the same process, and there is no per-test DB isolation. Symptom: `npm test` (full) intermittently fails `tests/services/*` because vitest runs test files in parallel against the one shared local Postgres and they truncate each other's rows (FK violations in `seedBase`); isolated `npm test -- <file>` always passes. Present since the DB tests landed (pre-Task 7). | Inject the client (pass `db` into services / a factory) where a second connection or per-test schema is needed; or run the DB suite with `--no-file-parallelism` / a per-file schema | Parallelism race CLEARED by `cbaa27b` (`vitest.config.ts` `fileParallelism: false` — files run serially against the shared local Postgres). Client-injection / per-test isolation still not scheduled (M1 uses one local Postgres for both). |
| 2026-08-31 | e2e specs skipped — no local Supabase; run with `supabase start` locally/CI. Cleared when CI added. | `tests/e2e/auth.spec.ts`, `tests/e2e/nav.spec.ts`, `tests/e2e/lead-pipeline.spec.ts`, `tests/e2e/settings.spec.ts`, `tests/e2e/dashboard.spec.ts` and `tests/e2e/products.spec.ts` (`test.describe.skip`) never assert the `/` → `/login` redirect, the sign-in flow, the role-aware nav rendering, the create-lead → pipeline-board flow, the owner-edits-threshold / SALES-redirected-off-`/settings` flow, the dashboard's funnel + today counts rendering after seeding, or (Task 8) the owner-sees-cost-columns catalogue + recommended-vs-current override/save flow + the SALES no-cost-columns redaction, in this env (no Docker/Supabase CLI/auth server; `.env.local` Supabase key is a placeholder). Task 7's nav is instead gated by `tests/domain/nav.test.ts` (pure `visibleNavItems` — Task 8 extends it: Products shown to both roles); Task 15's board join + move guard by `tests/services/pipeline.test.ts`; Task 19's save actions by `tests/services/settings-actions.test.ts`; Task 21's dashboard computation by `tests/domain/dashboard.test.ts` (pure `dashboardSummary`); Task 8's price-override / reset / redaction logic by `tests/services/product.test.ts` (Vitest). | Bring up `supabase start`, fill `.env.local` from `supabase status`, seed users via `scripts/create-user.ts`, run `npm run db:seed`, drop the `.skip` on all six specs — wire into CI | Post-M1 (CI pipeline) |
| 2026-09-01 | `src/app/(app)/page.tsx` is a deliberately thin M1 landing page — 4 read-only sections (Today counts, pipeline funnel, weighted pipeline value, hot-leads-needing-attention) | Not the spec's full Command Center: no six-block layout, no morning-plan / end-of-day modes, no date-range filter, no drill-down, no charts. It reuses the existing `getTodayView` / `listLeads` reads and adds one pure helper (`src/domain/dashboard.ts`). | Milestone 3 builds the full Command Center (six blocks, morning/eod) — it replaces this page's body; `dashboardSummary` stays as the funnel + weighted-value helper | Milestone 3 (Command Center) |
| 2026-08-31 | Route gate is per-layout `requireUser()` in `src/app/(app)/layout.tsx`, not middleware | A new top-level route group could forget to call `requireUser()` and ship unauthenticated; middleware only refreshes the auth cookie, it does not enforce | Centralise the gate in `src/middleware.ts` with a matcher that allows only `/login` + `/api/health` and redirects everything else when `getUser()` is null | Not scheduled (M1 has one gated group) |
| 2026-08-31 | `requireUser()` redirect branch has no unit test | The `redirect('/login')` path is only exercised by the (skipped) e2e spec; a regression that stops redirecting unauthenticated users would not fail `npm test` | Covered once the e2e auth spec runs in CI; or add a Next-router-aware test that asserts `redirect` is called | Post-M1 (CI pipeline) |
| 2026-08-31 | `overlapsExclusive(orgId, territoryId, excludeDistributorId?)` in `src/server/services/territory.ts` is a stub returning `false` | No `distributors` table exists in M1, so territory–distributor exclusivity conflicts cannot be detected; any caller that relies on it will treat every territory as conflict-free | Milestone 2 adds the `distributors` table with `territoryId` + `exclusive` — implement as a `select 1 from distributors where org_id = $1 and territory_id = $2 and exclusive and id <> $3 limit 1` | Milestone 2 (distributors table) |
| 2026-08-31 | Hand-edited migration `drizzle/0003_shiny_pride.sql` — the `activities_target_ck` CHECK (`lead_id IS NOT NULL OR distributor_id IS NOT NULL`) is appended by hand after `db:generate` | `drizzle-kit generate` does not emit it, so a future regenerate/squash of 0003 silently drops the constraint; the schema `crm.ts` file has no `check()` to diff against | Move the constraint into the `activities` `pgTable` definition as a Drizzle `check()` once the pinned drizzle-kit emits table CHECKs cleanly, then regenerate | Not scheduled (revisit on drizzle-kit upgrade) |
| 2026-09-01 | Dev-only auth escape hatch in `getSession()` (`DEV_LOGIN_EMAIL`, non-prod only) + `scripts/dev-fixtures.ts` | Local browsing without a Supabase Auth server; not a real auth path | Remove the hatch once Supabase Auth is wired; keep fixtures script as a dev convenience | — |
| 2026-09-01 | `setStage()` in `src/server/services/lead.ts` writes the pipeline-move timeline row with an inline `db.insert(activities).values(...)` | Duplicates the activity-insert logic that Task 14's `addActivity` service owns (type/occurredAt/org-scoping); a field added to `addActivity` won't reach this path | Replace the inline insert with a call to `addActivity(user, { leadId, type: 'OTHER', outcome })` once Task 14 lands | CLEARED by Task 14 — `setStage` now calls `addActivity(user, { leadId: id, type: 'OTHER', outcome })`; inline insert + `activities` import removed |
| 2026-09-01 | Pipeline board (`src/app/(app)/pipeline/`) has no LOST/ON_HOLD column — a drag to either would need a lost/on-hold reason, so `moveLeadAction` returns `{ error: 'open-detail' }` and `board.tsx` does `router.push('/leads/{id}')` instead of an inline modal | No on-board way to close/park a lead — the user is bounced to the detail page's `StageForm` to pick a reason. The board's `OPEN` list is `STAGES` minus `LOST`/`ON_HOLD` (12 columns); it deliberately does NOT reuse `OPEN_STAGES` from `domain/pipeline.ts` (which also drops `REPEAT_ORDER`). | Add an inline lost-reason modal to `board.tsx` (reason `<select>` over `LOST_REASONS` + notes) that calls a `setStage(..., { lostReason })`-capable action, then render a collapsed "Closed" strip | Post-M1 (pipeline polish) |
| 2026-09-01 | `Board` in `src/app/(app)/pipeline/board.tsx` seeds `useState(leads)` once; only the optimistic drag handler mutates it | An external change (another user restages a lead, a lead created elsewhere) is not shown until a full page reload — the post-move `revalidatePath('/pipeline')` refreshes the RSC payload but the client `useState` keeps its value | `useEffect(() => setItems(leads), [leads])`, a keyed remount on a server-provided version, or move to `useOptimistic` over server state | Post-M1 (pipeline polish) |
| 2026-09-01 | `deriveCounts()` in `src/server/services/dailyReport.ts` computes funnel counts (`qualifiedLeads`/`appointments`/`firstOrders`) from `distributor_leads.updated_at` falling on the report's IST day, as a proxy for "the lead moved to this stage that day" | A lead edited that day for any other reason (a field fix, a follow-up date change, a rescore) has its `updated_at` bumped and is double-counted; a stage move on day N followed by any edit on day N+1 drops out of day N's count. `newLeads` (from `created_at`) is exact; only the stage-movement rows are approximate. | Derive the counts from `audit_log` `setStage` rows (`entity='lead'`, `action='setStage'`, `created_at` in the IST day) — one row per actual stage transition, immune to unrelated edits | Milestone 3 (audit-log-derived reporting) |
| 2026-09-01 | Settings screen (`src/app/(app)/settings/`) edits only `scoreWeights` + `hotLeadProbabilityThreshold`; the `stageProbability` map is rendered read-only (`<pre>` of `CONFIG_DEFAULTS.stageProbability`) with a `// ponytail:` note in `page.tsx` | An owner cannot retune per-stage win probabilities from the UI — the weighted pipeline totals + hot-lead bucketing use the hard-coded defaults until a code change | M2: add a 14-row stage-probability editor (one 0–100 input per `LeadStage`) with its own `saveStageProbabilities` action — validate every stage present & in range, `setConfig(orgId,'stageProbability',...)`, then a rescore/So-what sweep so open leads pick up the new probabilities | Milestone 2 (config editor expansion) |
| 2026-09-01 | `PRODUCT_FINANCIAL_FIELDS` in `src/server/services/product.ts` is a hard-coded 3-name list (`ssBillingPrice`, `floorPrice`, `targetPrice`) driving `redactPrice`/`redactProduct`/`redactProducts` for the SALES role | The SALES cost-redaction boundary on the product read path only strips these 3 columns; a cost column added to `product_prices` (or surfaced onto `ProductWithPrice`) before its name is appended here would leak to SALES | If M2b/quotations add product cost columns to the read path (freight/loading/scheme snapshots, landed cost, etc.), append each name to `PRODUCT_FINANCIAL_FIELDS` — the wiring (`listProducts` returns full rows; screens wrap in `redactProducts`) already redacts whatever is listed | Milestone 2b / quotations (product cost columns on the read path) |
| 2026-09-01 | `regenerateAllRecommended` in `src/server/services/product.ts` calls `bandsForCategory(orgId, categoryName)` once per product row (2 `getConfig` reads each) inside the loop | O(n) config reads — ~368 extra round-trips for the ~184-SKU F&F catalogue on a rare owner-triggered action; fine at that size, wasteful if the catalogue grows or the action is automated | Hoist `getConfig('pricingBands')` + `getConfig('pricingBandsByCategory')` out of the loop and merge per row inline (duplicates `bandsForCategory`'s 2 lines), or add a memoised `bandsForCategory` | Not scheduled (revisit if the catalogue outgrows a few hundred SKUs or regeneration is scheduled) |
| 2026-09-01 | `LEAD_FINANCIAL_FIELDS` in `src/server/services/lead.ts` is `[]` — the SALES redaction boundary (`redactLead`/`redactLeads`, wired into both lead read screens by I6) is live but strips nothing | M1 has no cost columns on the lead read path, so the redaction is a no-op today; a financial field added to `LeadRow` before its name is listed here would leak to SALES | M2 adds `ssBillingPrice`/`floorPrice` etc. to the lead read path — add each name to `LEAD_FINANCIAL_FIELDS` (the wiring already redacts it); also route EVERY lead read path that returns rows outside `redactLead`/`redactLeads` through it — currently `boardLeads` (see its `// ponytail:` note) and the dashboard's `listLeads(orgId,{limit:500})` in `src/app/(app)/page.tsx` (aggregate-only today, so no per-lead cost is rendered) | Milestone 2 (lead/product cost columns) |
| 2026-09-01 | No CSV product import / export UI (spec §40) — the catalogue is loaded only by `seedCatalogue` (`npm run db:seed:catalogue` / `dev:fixtures`); there is no owner-facing bulk upload, edit, or download screen | An owner cannot add/replace SKUs or bulk-edit prices from the app — every catalogue or price change beyond the per-SKU Products screen needs a code change + re-seed | M3 builds the CSV product import/export screen (upload → validate → diff → apply; export current catalogue + prices) per spec §40 | Milestone 3 (CSV import/export) |
| 2026-09-01 | `gstPctByCategory` in `data/ff-catalogue.json` (Dry Fruits 12, Seeds/Flours/Spices/Other 5) is a best-guess slab per category, not sourced from F&F | Wrong GST on a category skews the ex-GST taxable value and max-permissible-discount in the pricing calculator for every SKU in it; `gst_pct` is per-product in the schema so a one-off fix is cheap, a slab-wide one is a re-seed | Confirm the actual GST slab per category with F&F; correct the JSON map (and any per-SKU exceptions) and re-run `db:seed:catalogue` on a fresh org, or bulk-`UPDATE products.gst_pct` | Pending F&F confirmation |
| 2026-09-01 | `product_prices.mrp` is a snapshot copy of `products.mrp` written at seed / reset time; `products.mrp` stays canonical (the pricing waterfall reads it, not the snapshot) | The two can drift — nothing keeps `product_prices.mrp` in sync when `products.mrp` changes, and there is no MRP editor yet so today they always match | When MRP editing lands (M2b/M3), have that write path update both columns (or drop the snapshot and always read `products.mrp`) | Milestone 2b / M3 (MRP editing) |
| 2026-09-01 | ~~For volatile SKUs the default `volatileFloorBufferPct` (12) equals `ssNormalMarginPct` (12), so `recommendPricing` returns `floorPrice === distributorPrice`~~ **RESOLVED** (M2a final-review) — default lowered to `volatileFloorBufferPct: 10` in `CONFIG_DEFAULTS.pricingBands`, so a volatile SKU's recommended floor now sits strictly below its distributor price (cost×1.10 < cost×1.12). Still owner-tunable in Settings → Pricing bands. | — | — | Cleared by M2a final-review fixes (`src/server/services/config.ts`) |
| 2026-09-02 | Hand-edited migration `drizzle/0010_yellow_loa.sql` — the `quotations_party_ck` CHECK (`(lead_id IS NOT NULL) <> (distributor_id IS NOT NULL)`) is appended by hand after `db:generate` | `drizzle-kit generate` does not emit it, so a regenerate/squash of 0010 silently drops the lead-XOR-distributor guard; `schema/quotation.ts` has no `check()` to diff against | Move the constraint into the `quotations` `pgTable` definition as a Drizzle `check()` once the pinned drizzle-kit emits table CHECKs cleanly, then regenerate. Mirrors the `activities_target_ck` row above | Not scheduled (revisit on drizzle-kit upgrade) |
| 2026-09-02 | `allocateQuoteNo` in `src/server/services/quotation.ts` builds `Q-YYYYMM-NNN` from a non-transactional `count()+1` over that month's rows | Two `createQuotation` calls in the same month racing between the `count()` and the insert get the same `quoteNo`; the `quotations_org_quote_no_idx` unique index then fails the second insert. Fine for a single owner working serially; unsafe under concurrent quote creation | Back the per-org monthly sequence with a real Postgres `SEQUENCE` (or an `INSERT ... ON CONFLICT` retry loop, or a `quote_counters` row locked `FOR UPDATE`) | Phase 2 / when multiple users create quotes concurrently |
| 2026-09-02 | Quotation line items have no edit path in M2b — `quotation` service exposes `create` / `submit` / `approve` / `setStatus` but no `updateItem` / `addItem` / `removeItem`; the `/quotations/[id]` detail screen renders items read-only | To fix a wrong qty/rate/product on a DRAFT quote the user must recreate the whole quotation (and it burns a `quoteNo`). No way to revise a single line | Add `updateQuotationItem` / `addQuotationItem` / `removeQuotationItem` (DRAFT-only, re-run `classifyRate` + scheme auto-apply, re-snapshot rates, audited) and an inline editor on the detail screen | Phase 2 (quotation editing) |
| 2026-09-02 | `DISTRIBUTOR_INCENTIVE` schemes are fully defined (domain eligibility + benefit math in `src/domain/scheme.ts`, creatable in the Schemes screen) but no benefit is ever **accrued or paid** — nothing writes a `scheme_applications` row or a payable for an incentive scheme | An owner can configure a distributor incentive, but the system never tracks what is owed or pays it out; only FLAT_DISCOUNT / QTY_SCHEME benefits reach `quotation_items` / `scheme_applications` | Phase 2 adds incentive accrual (period aggregation of a distributor's qualifying purchases → `scheme_applications` / a payable ledger) once Orders exist to aggregate against | Phase 2 (incentive accrual, needs Orders) |
| 2026-09-02 | `/quotations/[id]/print` renders inside the `(app)` route group, so it inherits the `AppNav` sidebar + header chrome (`// ponytail:` note in the page) | The "print / WhatsApp" view is not chrome-less — printing it or screenshotting for WhatsApp includes the app navigation | Move the route to its own group outside `(app)` (its own minimal layout) or add a dedicated print stylesheet that hides the chrome | Milestone 3 (print/share polish) |
| 2026-09-02 | Quotation net totals are summed row-by-row in JS, not in SQL: `/quotations` list calls `getQuotation` per row and reduces `items.netAmount` (`// ponytail:` N+1 note in `page.tsx`); the detail + print pages and per-quote pending-approval filtering (`listPendingApprovals(orgId).filter(...)`) likewise compute in the app layer | O(rows) extra reads to render the list and O(approvals) to show one quote's pending set — fine at M2b volume, wasteful as quotations/approvals grow | Add a `quotationTotals(orgId)` aggregate (SQL `sum(net_amount) group by quotation_id`) for the list, and a `listPendingApprovals(orgId, { quotationId })` filtered query for the detail page | Not scheduled (revisit if quote volume grows) |
| 2026-09-02 | `createQuotation` / `submitQuotation` in `src/server/services/quotation.ts` do their multi-row work (quotation insert + N item inserts + N `price_approvals` + `scheme_applications` + audit) as separate un-transacted statements — no `db.transaction(...)` wrapper | A failure partway (constraint, connection drop) leaves a half-written quote — items without their parent's approvals, or an audit row for work that didn't fully land | Wrap each service entry point's writes in `db.transaction(async (tx) => { ... })` and thread `tx` through the inserts + `writeAudit` | Phase 2 (transaction pass across services) |
| 2026-09-01 | `regenerateAllRecommended` ALWAYS skips manually-overridden price rows (`manualOverride = false` filter is unconditional); the `onlyUnoverridden` opt and the Settings + `/products` "only where not manually overridden" checkbox are accepted/rendered but inert | An owner cannot bulk-force a recompute over hand-entered prices from the UI — a manual override is sticky until someone resets that row individually. Deliberate: there is no per-price history table yet, so a bulk rewrite of overridden rows would lose the hand-entered values with only a coarse audit trail. | M3 adds per-price history (before/after per column per change). Then wire a real force path behind `onlyUnoverridden: false` (drop the unconditional filter when the opt is false) and make the checkbox live. | Milestone 3 (per-price history) |
| 2026-09-05 | `commandCenterSummary` (`src/server/services/commandCenter.ts`) hardcodes `yesterday.activityCount`, `yesterday.quotationsSentValue`, and `kpis.openQuotationsValue` to `0` — each has its own `// ponytail:` comment explaining the deferred aggregate query — and `src/app/(app)/page.tsx` (Task 9) does not render any of the three fields at all, not just show a `0` | An org-wide "activities logged yesterday" count, a "quotations sent value (₹)" sum, and an "open quotations value (₹)" sum all need a proper cross-employee / cross-quotation aggregate (a `sum(net_amount)` join, in the case of the two value fields) that Task 8 deliberately did not add, to keep that task's query count bounded. Task 9 then made the further deliberate call to omit all three from the Dashboard UI entirely — a permanently-`0` "Quotations sent value" / "Activity count" / "Open quotations value" stat on the owner's daily home screen would read as real data while being fake, which is worse than not showing it. So this is two layered decisions: the service returns `0`/stubbed, and the UI doesn't surface those `0`s at all. | Add the real aggregate queries (a `sum(net_amount) group by quotation_id`-style join for the two value fields, a cross-employee `activities` count for the day, matching the same pattern already logged for the quotation list screen's N+1 totals) to `commandCenterSummary`, then add the three `Stat` tiles to the relevant `/` sections | Not scheduled (Phase 2-adjacent follow-up) |
| 2026-09-05 | Command Center and "Executive Dashboard" are merged into the single existing `/` route (`src/app/(app)/page.tsx`) — there is no separate `/dashboard` or `/executive-dashboard` route | The spec (`docs/superpowers/specs/2026-08-31-super-stockist-design.md` §6.2/§6.3) describes Command Center and Executive Dashboard as two spec sections, but the spec's own §6.1 navigation list has a single "Dashboard" nav entry, not two. The M3 plan (`docs/superpowers/plans/2026-09-04-super-stockist-milestone-3-command-center.md`, Architecture line) made the deliberate call to treat §6.2's six blocks and §6.3's Executive Dashboard content (KPI cards, distributor funnel) as one page rather than invent a second nav item with no corresponding link anywhere in the app. `/` now renders both: the morning/EOD block sections plus a "Key numbers" KPI-card row and a "Pipeline funnel" block. | If a future spec revision genuinely wants two distinct owner-facing screens (e.g. a lighter daily Command Center vs. a heavier drill-down Executive Dashboard), split `/` into `/` (Command Center) + `/dashboard` (Executive Dashboard) and move the KPI/funnel sections into the new route | Not scheduled (revisit only if the spec explicitly asks for two routes) |
| 2026-09-05 | Every money/inventory/order-dependent block listed in the spec for Command Center, Executive Dashboard, and Reports was dropped wholesale this milestone, not stubbed: from §6.2's six blocks — yesterday's sales (₹) and orders count, today's "orders in", forecasted "expected orders (7/30d)", the entire "Where is my money?" block (inventory value, committed value, outstanding, overdue), and "activation rate"/"repeat rate" under "Where is my growth?"; from §6.2's "What needs my attention?" — overdue payments, stock-outs, near-expiry (kept: hot lead no next action, exclusivity conflict, missing daily report, pending price approvals); from §6.3 Executive Dashboard — sales charts (daily/weekly/monthly by distributor/category/SKU), inventory alerts, distributor health; from §6.3 Reports — gross margin, contribution, AOV, repeat rate, the Monthly Business Review composite; from §7's 10 listed alert-scan categories — reorder-due distributors, payments due/overdue, stock below reorder level, near-expiry (kept 6 of 10: follow-ups due/overdue, quotations stale, distributor review due, inactive distributors, empty territories, missing daily reports) | None of these render anywhere in this milestone's Command Center, Reports, or alert scan — per the plan's Global Constraints ("No Phase-2 stubs. Do not add UI placeholders, 'coming soon' cards, or zeroed-out widgets for money/inventory/order data"), because none of Orders, Inventory, or Receivables/Collections has a schema yet (`docs/superpowers/specs/2026-08-31-super-stockist-design.md` §4.7-§4.9 mark them Phase 2) | Add with Phase 2 Orders/Inventory schema, reusing the same KPI-card/block components (`Stat` in `src/app/(app)/page.tsx`, the report-table pattern in `src/server/services/reports.ts` + `src/app/(app)/reports/*`, the alert-candidate pattern in `src/domain/alerts.ts` + `src/server/services/notification.ts`) | Phase 2 (Orders/Inventory schema) |
| 2026-09-05 | Global filters (Tasks 10/12 — date range, territory, employee) apply only to the four Reports screens (`/reports/pipeline`, `/reports/quotations`, `/reports/employees`, `/reports/distributors`); the Command Center/Dashboard on `/` is unfiltered — `commandCenterSummary(user, mode)` takes no `ReportFilters` param | This is a deliberate scope decision, not an oversight: the spec's §6.4 line says filters apply to "Executive Dashboard and Reports," but since Executive Dashboard content is merged into `/` (see the merge row above) and `/` is driven entirely by `commandCenterSummary`, wiring filters into it would mean every one of that service's ~10 parallel queries takes a `ReportFilters` param — a real scope increase beyond what Tasks 8/9 were briefed to do. The plan's Self-Review section flagged this gap explicitly and the user confirmed before dispatch: "global filters stay Reports-only (Tasks 10/12); the Command Center/Dashboard on `/` stays unfiltered — do not expand Task 8/9 to add filter params. This is not a defect to fix during execution" (`.superpowers/sdd/2026-09-04-super-stockist-milestone-3-command-center/progress.md`). **Update (final-review fix wave, 2026-09-05):** within the four Reports screens themselves, `pipelineReport`/`quotationsReport`/`distributorsReport` previously ignored most of `from`/`to`/`territoryId`/`employeeId` (only `employeesReport` applied `from`/`to`) even though `<GlobalFilters>` rendered the full control set on all four — that gap is now fixed, and each function applies every filter that's meaningful for it (see `src/server/services/reports.ts`). `categoryId` is accepted by `parseFilters` but never rendered as a control in `<GlobalFilters>`, and is deliberately left unapplied by `pipelineReport`/`distributorsReport` — leads/distributors have no reliable `categoryId` association (only an unstructured, unpopulated `current_categories`/`product_categories` jsonb field), documented inline at each call site. One residual: `/reports/employees` still renders the Territory control from `<GlobalFilters>`, but `employeesReport` does not apply `territoryId` — an employee has no direct territory column, only the time-boxed `territory_assignments` table (fromDate/toDate), which needs "current assignment as of the report window" logic rather than a plain equality filter; documented inline at the call site (`src/server/services/reports.ts`). | Thread `filters: ReportFilters` through `commandCenterSummary` and its ~10 constituent queries (`listLeads`, `listQuotations`, `listDistributors`, etc.), and add a `<GlobalFilters>` bar to `/`. Separately, if `categoryId` is ever wanted on pipeline/distributors cuts, it needs a real join path (e.g. via quotation line items' products) or a real category column on leads/distributors — neither exists today. | Not scheduled (confirmed scope decision, revisit only if the owner explicitly asks for filtered dashboard KPIs) |
| 2026-09-05 | Notifications re-raise daily for a persisting condition (no auto-resolve when the underlying condition clears — e.g. a follow-up that's still overdue tomorrow raises a fresh `follow_up_overdue` row every day, `dedupeDate`-deduped per day but never suppressed once created), and there is no dedicated `/notifications` page with pagination — only the header bell's panel (capped at a fixed fetch limit) and a "mark all read" action | This milestone's Notification Center is deliberately scoped to raise + read/unread + mark-(all)-read; a real retention policy (auto-resolve a category+entity's open notification once the condition clears, e.g. the lead gets a next-follow-up date, the report gets submitted) and a full paginated list view were out of scope for the final fix wave, which only closed the badge/panel-count mismatch and added mark-all-read | Design and add an auto-resolve path (e.g. `runAlertScan` also marks-read/deletes any notification whose category+entity no longer matches an active candidate) plus a `/notifications` route with real pagination, replacing the bell panel's fixed-limit fetch | Not scheduled (deferred to a future milestone) |
| 2026-09-05 | `wrangler.toml`'s `[triggers] crons = ["0 2 * * *"]` block is declared but not actually wired to anything — Cloudflare invokes a scheduled trigger via the Worker's `scheduled()` export, and the stock OpenNext build (`.open-next/worker.js`) only exports `fetch`, with no custom entrypoint added in this repo | Once deployed, the nightly trigger will fire but invoke nothing — the alert scan will still only run via the manual "Run alert scan" button in Settings, exactly as it did before the trigger config was added. Declaring the block now is harmless (inert until deployed) and keeps the intended cadence documented, but by itself it does not make the scan automatic | Add a custom Worker entrypoint (or an OpenNext Cloudflare `scheduled` hook, if the pinned `@opennextjs/cloudflare` version supports one) that calls `runAlertScan` for every org the same way `/api/cron/alerts` does today, wired to the Cloudflare Cron Trigger event | Not scheduled (deploy-time work, deferred with the rest of Cloudflare deployment) |
